---
- name: Setup Minimal Sway Environment (Universal)
  hosts: localhost
  connection: local
  become: true
  tasks:

    - name: Install core packages, Sway, and desktop utilities
      apt:
        name:
          - fastfetch
          - btrfsmaintenance
          - lxqt-policykit
          - sway-notification-center
          - thunar
          - distrobox
          - sway
          - swaybg
          - swayidle
          - swaylock
          - grimshot
          - foot
          - pipewire
          - wireplumber
          - pavucontrol
          - network-manager
          - systemd-zram-generator
          - xdg-desktop-portal-wlr
          - xdg-desktop-portal-gtk
          - fuzzel
          - git
          - flatpak
          - xwayland
          - firefox-esr
          - gamemode
          - fonts-font-awesome
          - fonts-noto-core
          - fonts-noto-mono
          - fonts-noto-color-emoji
          - gnome-disk-utility
          - network-manager-applet
          - power-profiles-daemon
          - ufw
          - cups
          - vim
          - stow
          - htop
          - waybar
          - libspa-0.2-bluetooth
          - bluez
          - blueman
        state: present
        update_cache: yes
        cache_valid_time: 3600

    - name: Enable Flathub repository for the local user
      become: false
      flatpak_remote:
        name: flathub
        state: present
        method: user
        flatpakrepo_url: https://dl.flathub.org/repo/flathub.flatpakrepo

    - name: Install host-specific Flatpaks (Safe Lookup)
      become: false
      flatpak:
        name: "{{ item }}"
        method: user
        state: present
        remote: flathub
      # 1. Lookup the file (returns a string)
      # 2. Split by newline to create a list
      # 3. Reject empty lines (in case there's a trailing newline in the file)
      loop: "{{ lookup('file', ansible_hostname + '/flatpak.txt', errors='ignore').split('\n') | reject('equalto', '') | list }}"
      when: lookup('file', ansible_hostname + '/flatpak.txt', errors='ignore') != ""

    - name: Configure NetworkManager
      block:
        - name: Set NetworkManager to managed=true
          ini_file:
            path: /etc/NetworkManager/NetworkManager.conf
            section: ifupdown
            option: managed
            value: "true"

        - name: Comment out physical interfaces in legacy config
          replace:
            path: /etc/network/interfaces
            regexp: '^(allow-hotplug|iface|auto) (enp|wlp|eth|wlan)'
            replace: '# \1 \2'

        - name: Restart NetworkManager
          systemd:
            name: NetworkManager
            state: restarted

    - name: Configure systemd-zram-generator
      copy:
        dest: /etc/systemd/zram-generator.conf
        content: |
          [zram0]
          zram-size = min(ram / 2, 4096)
          compression-algorithm = zstd
          swap-priority = 100
        owner: root
        group: root
        mode: '0644'
      register: zram_config

    - name: Refresh systemd generator
      systemd:
        daemon_reload: yes
      when: zram_config.changed

    - name: Purge incorrect root entries from fstab
      lineinfile:
        path: /etc/fstab
        # Removes any /dev/mapper root lines that DO NOT match the current hostname
        regexp: "^/dev/mapper/(?!{{ ansible_hostname }}--vg-root).+--vg-root"
        state: absent

    - name: Optimize Btrfs Mounts
      mount:
        path: "{{ item.mnt }}"
        src: "{{ item.dev }}"
        fstype: btrfs
        opts: "{{ item.opts }}"
        dump: "0"
        passno: "0"
        state: present
      loop:
        - { dev: "/dev/mapper/{{ ansible_hostname }}--vg-root", mnt: '/', opts: 'defaults,subvol=@rootfs,noatime,compress=zstd:1,ssd,discard=async,space_cache=v2,autodefrag' }
        - { dev: 'UUID=5efd422d-9ce0-49a4-b4b7-dfba42d9e434', mnt: '/mnt/z', opts: 'defaults,noatime,compress=zstd:1,ssd,discard=async,space_cache=v2,autodefrag,nosuid,nodev,nofail,x-gvfs-show,x-gvfs-name=z' }
        - { dev: 'UUID=d59767b0-cc74-4acc-93fb-25e24f53449e', mnt: '/mnt/g', opts: 'defaults,noatime,compress=zstd:1,ssd,discard=async,space_cache=v2,autodefrag,nosuid,nodev,nofail,x-gvfs-show,x-gvfs-name=g' }

    - name: Configure btrfsmaintenance
      lineinfile:
        path: /etc/default/btrfsmaintenance
        regexp: "^{{ item.key }}="
        line: '{{ item.key }}="{{ item.value }}"'
      loop:
        - { key: 'BTRFS_BALANCE_PERIOD', value: 'monthly' }
        - { key: 'BTRFS_SCRUB_PERIOD', value: 'monthly' }
        - { key: 'BTRFS_TRIM_PERIOD', value: 'none' }
      notify: Refresh btrfsmaintenance

    - name: Remove PulseAudio Bluetooth conflict
      apt:
        name: pulseaudio-module-bluetooth
        state: absent
        purge: yes

    - name: Enable Bluetooth
      systemd:
        name: bluetooth
        enabled: yes
        state: started

    - name: Enable lingering for local user (Fixes --user systemd)
      command: "loginctl enable-linger {{ ansible_user_id }}"
      args:
        creates: "/var/lib/systemd/linger/{{ ansible_user_id }}"

    - name: Ensure User PipeWire services are running
      become: false
      systemd:
        name: "{{ item }}"
        state: started
        enabled: yes
        scope: user
      loop:
        - pipewire
        - pipewire-pulse
        - wireplumber

    - name: Configure Firewall Rules
      ufw:
        rule: allow
        port: "{{ item.port }}"
        proto: "{{ item.proto }}"
      loop:
        - { port: '22000', proto: 'tcp' }
        - { port: '22000', proto: 'udp' }
        - { port: '21027', proto: 'udp' }

    - name: Enable Firewall
      ufw:
        state: enabled

  handlers:
    - name: Refresh btrfsmaintenance
      shell: |
        if [ -x /usr/share/btrfsmaintenance/btrfsmaintenance-refresh-cron.sh ]; then
          /usr/share/btrfsmaintenance/btrfsmaintenance-refresh-cron.sh
        fi
        systemctl daemon-reload
